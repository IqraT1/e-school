# HTML
# Archive your static HTML project and save it with the build record.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: windows-latest

variables:
  buildConfiguration: 'Debug'
  buildPlatform: 'Any CPU'
  continueOnError: true
stages:
  - stage: CI
    displayName: CI/CD of eschool
    jobs:
       - job: Build
         steps:
            - task: ArchiveFiles@2
              displayName: Archive Static Files
              inputs:
                  rootFolderOrFile: '$(build.sourcesDirectory)/s'
                  archiveType: 'zip'
                  archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'
                  replaceExistingArchive: true
                  includeRootFolder: false
            - task: PublishBuildArtifacts@1
              inputs:
                PathtoPublish: '$(build.sourcesDirectory)/a'
                ArtifactName: 'CloudSigma'
                publishLocation: 'Container'

       - job: 
       - deployment: VMDeploy
         dependsOn: Build
         displayName: Deploy to VM
         environment: 
                name: CloudSigma
                resourceType: VirtualMachine
                tags: dev, web # only deploy to virtual machines with this tag
         strategy:
                runOnce:
                  deploy:   
                      steps:
                      - task: DownloadPipelineArtifact@2
                        inputs:
                          buildType: 'current'
                          artifactName: 'CloudSigma'
                          targetPath: '$(Pipeline.Workspace)/CloudSigma'

                      - task: IISWebAppManagementOnMachineGroup@0
                        continueOnError: false 
                        displayName: IISWebAppManage
                        inputs:
                           IISDeploymentType: 'IISWebsite'
                           ActionIISWebsite: 'CreateOrUpdateWebsite'
                           WebsiteName: 'Default Web Site'
                           WebsitePhysicalPath: 'C:\ESchool\Deployment'
                           WebsitePhysicalPathAuth: 'WebsiteUserPassThrough'
                          
                      - task: IISWebAppDeploymentOnMachineGroup@0
                        continueOnError: false 
                        displayName: IISWebAppDeploy
                        inputs:
                              WebSiteName: 'Default Web Site'
                              VirtualApplication: 'CloudSigma'
                              Package: '$(System.DefaultWorkingDirectory)/CloudSigma/$(Build.BuildId).zip'
                              TakeAppOfflineFlag: true
                              XmlVariableSubstitution: true 
                      

